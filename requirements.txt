# ComfyUI normally already provides torch, torchaudio, transformers, tokenizers,
# safetensors, and huggingface_hub. This nodepack intentionally does not pin or
# upgrade torch/transformers because the native path works against the Qwen3
# modules available in Transformers 5.3.0.
accelerate
numpy
safetensors
tokenizers
huggingface_hub
